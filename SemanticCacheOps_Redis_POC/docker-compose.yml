services:
  redis:
    image: redis/redis-stack-server:latest
    container_name: semanticcacheops-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
